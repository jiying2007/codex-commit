name: Cleanup scope inference v1.2.4

on:
  pull_request:
    branches: [main]
    types: [opened]

permissions:
  contents: read

jobs:
  cleanup:
    if: github.head_ref == 'cleanup-scope-v1.2.4-trigger' && github.event.pull_request.head.repo.full_name == github.repository
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true
      - name: Remove temporary workflow and branches
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/cleanup-scope-v1.2.4.yml
          git commit -m 'build: remove one-time scope cleanup runner'
          git push origin HEAD:main
          gh pr close "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" || true
          git push origin --delete fix/scope-inference-v1.2.4 || true
          git push origin --delete cleanup-scope-v1.2.4-trigger || true
